# Enflasyon Canavarı — Temel (Faz 0) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Projenin çalışan iskeletini kur — npm workspaces (web + data), docs notları, Zod şema + veri doğrulama (TDD ile), örnek veri ve veriyi yükleyip listeleyen bir Next.js kabuğu.

**Architecture:** Tek repo, npm workspaces ile iki paket: `data/` (`@ec/data` — şemalar, tipler, JSON veri, doğrulama) ve `web/` (`@ec/web` — Next.js App Router frontend). `web`, `@ec/data`'yı doğrudan import eder; Next.js `transpilePackages` ile TS kaynağını derler, ayrı build adımı gerekmez. Veri doğru olmadan güven olmaz, bu yüzden şema ve veri bütünlüğü TDD ile yazılır.

**Tech Stack:** Node 24, npm 11 workspaces, TypeScript, Zod (şema), Vitest (test), tsx (CLI script), Next.js 16 (App Router) + Tailwind v4.

---

## Dosya Yapısı (bu planın ürettiği)

```
enflasyon-canavari/
├── package.json                 # workspaces: ["data", "web"]
├── tsconfig.base.json           # ortak TS ayarları
├── vitest.config.ts             # kök test ayarı (data paketi testleri)
├── README.md
├── docs/                        # notlar (md)
│   ├── 00-vizyon.md ... 07-yol-haritasi.md
│   ├── kararlar/0001-stack-secimi.md
│   └── superpowers/...          # spec + bu plan (mevcut)
├── data/                        # @ec/data
│   ├── package.json
│   ├── README.md
│   ├── src/
│   │   ├── index.ts             # public export
│   │   ├── schema.ts            # Zod: Item, PricePoint, Source
│   │   ├── types.ts             # türetilmiş TS tipleri
│   │   ├── load.ts              # JSON'ları okuyup parse eden yükleyici
│   │   └── validate.ts          # bütünlük doğrulaması (referans/etiket/kaynak)
│   ├── items/items.json
│   ├── sources/sources.json
│   ├── prices/gida.json
│   └── test/
│       ├── schema.test.ts
│       └── validate.test.ts
├── scripts/
│   └── validate-data.ts         # CLI: tüm veriyi doğrula
└── web/                         # @ec/web (Next.js)
    ├── package.json
    ├── next.config.ts
    ├── app/(layout/page/kaynaklar)
    ├── components/AdSlot.tsx
    ├── config/flags.ts
    └── test/AdSlot.test.tsx
```

---

## Task 1: Kök workspace + araçlar

**Files:**
- Create: `package.json`
- Create: `tsconfig.base.json`
- Create: `vitest.config.ts`

- [ ] **Step 1: Kök package.json oluştur**

`package.json`:
```json
{
  "name": "enflasyon-canavari",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["data", "web"],
  "scripts": {
    "test": "vitest run",
    "validate:data": "tsx scripts/validate-data.ts",
    "dev": "npm run dev -w web"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0",
    "zod": "^3.23.0"
  }
}
```

- [ ] **Step 2: Ortak tsconfig oluştur**

`tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true,
    "noUncheckedIndexedAccess": true
  }
}
```

- [ ] **Step 3: Vitest yapılandırması oluştur**

`vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: ["data/test/**/*.test.ts"],
    environment: "node",
  },
});
```

- [ ] **Step 4: Bağımlılıkları kur**

Run: `npm install`
Expected: `node_modules/` oluşur, hata yok. `package-lock.json` yazılır.

- [ ] **Step 5: Commit**

```bash
git add package.json tsconfig.base.json vitest.config.ts package-lock.json
git commit -m "chore: kök workspace ve test araçlarını kur"
```

---

## Task 2: data paketi — Zod şema (TDD)

**Files:**
- Create: `data/package.json`
- Create: `data/src/schema.ts`
- Create: `data/src/types.ts`
- Create: `data/src/index.ts`
- Test: `data/test/schema.test.ts`

- [ ] **Step 1: data paketi package.json oluştur**

`data/package.json`:
```json
{
  "name": "@ec/data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
```

- [ ] **Step 2: data paketi tsconfig oluştur**

`data/tsconfig.json`:
```json
{
  "extends": "../tsconfig.base.json",
  "include": ["src", "test"]
}
```

- [ ] **Step 3: Başarısız testi yaz**

`data/test/schema.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { ItemSchema, PricePointSchema } from "../src/schema";

describe("ItemSchema", () => {
  it("geçerli bir kalemi kabul eder", () => {
    const ok = ItemSchema.safeParse({
      id: "ekmek",
      isim: "Ekmek",
      kategori: "gida",
      birim: "adet",
      ikon: "bread",
      aciklama: "300g somun ekmek",
    });
    expect(ok.success).toBe(true);
  });

  it("bilinmeyen kategoriyi reddeder", () => {
    const bad = ItemSchema.safeParse({
      id: "ekmek",
      isim: "Ekmek",
      kategori: "uzay",
      birim: "adet",
      ikon: "bread",
      aciklama: "x",
    });
    expect(bad.success).toBe(false);
  });
});

describe("PricePointSchema", () => {
  it("kaynaklı ve etiketli noktayı kabul eder", () => {
    const ok = PricePointSchema.safeParse({
      itemId: "ekmek",
      yil: 2005,
      deger: 0.4,
      tip: "ortalama",
      kaynakTipi: "elle",
      kaynakAdi: "TÜİK",
      kaynakURL: "https://www.tuik.gov.tr",
      dogrulama: "dogrulanmis",
    });
    expect(ok.success).toBe(true);
  });

  it("2005'ten önceki yılı reddeder", () => {
    const bad = PricePointSchema.safeParse({
      itemId: "ekmek",
      yil: 2004,
      deger: 0.4,
      tip: "ortalama",
      kaynakTipi: "elle",
      kaynakAdi: "TÜİK",
      kaynakURL: "https://www.tuik.gov.tr",
      dogrulama: "dogrulanmis",
    });
    expect(bad.success).toBe(false);
  });

  it("negatif değeri reddeder", () => {
    const bad = PricePointSchema.safeParse({
      itemId: "ekmek",
      yil: 2010,
      deger: -1,
      tip: "net",
      kaynakTipi: "elle",
      kaynakAdi: "TÜİK",
      kaynakURL: "https://www.tuik.gov.tr",
      dogrulama: "dogrulanmis",
    });
    expect(bad.success).toBe(false);
  });
});
```

- [ ] **Step 4: Testi çalıştır, başarısız olduğunu gör**

Run: `npx vitest run data/test/schema.test.ts`
Expected: FAIL — `../src/schema` bulunamıyor / export yok.

- [ ] **Step 5: Şemayı yaz**

`data/src/schema.ts`:
```ts
import { z } from "zod";

export const KATEGORILER = [
  "gida",
  "doviz",
  "altin",
  "akaryakit",
  "fatura",
  "ulasim",
  "barinma",
  "capa",
] as const;

export const BIRIMLER = [
  "adet",
  "kg",
  "litre",
  "gram",
  "USD",
  "EUR",
  "endeks",
  "TL",
  "kWh",
  "m2",
  "m3",
] as const;

export const ItemSchema = z.object({
  id: z.string().min(1),
  isim: z.string().min(1),
  kategori: z.enum(KATEGORILER),
  birim: z.enum(BIRIMLER),
  ikon: z.string().min(1),
  aciklama: z.string().default(""),
});

export const PricePointSchema = z.object({
  itemId: z.string().min(1),
  yil: z.number().int().min(2005).max(2100),
  deger: z.number().positive(),
  tip: z.enum(["net", "ortalama"]),
  kaynakTipi: z.enum(["resmi-api", "elle", "epdk", "crowdsource"]),
  kaynakAdi: z.string().min(1),
  kaynakURL: z.string().url(),
  dogrulama: z.enum(["dogrulanmis", "beklemede"]),
  not: z.string().optional(),
});

export const SourceSchema = z.object({
  ad: z.string().min(1),
  tip: z.enum(["resmi-api", "elle", "epdk", "crowdsource"]),
  url: z.string().url(),
  metodoloji: z.string().min(1),
});
```

- [ ] **Step 6: Tipleri ve index'i yaz**

`data/src/types.ts`:
```ts
import { z } from "zod";
import { ItemSchema, PricePointSchema, SourceSchema } from "./schema";

export type Item = z.infer<typeof ItemSchema>;
export type PricePoint = z.infer<typeof PricePointSchema>;
export type Source = z.infer<typeof SourceSchema>;
```

`data/src/index.ts`:
```ts
export { ItemSchema, PricePointSchema, SourceSchema, KATEGORILER, BIRIMLER } from "./schema";
export type { Item, PricePoint, Source } from "./types";
```

- [ ] **Step 7: Testi çalıştır, geçtiğini gör**

Run: `npx vitest run data/test/schema.test.ts`
Expected: PASS (5 test).

- [ ] **Step 8: Commit**

```bash
git add data/package.json data/tsconfig.json data/src/schema.ts data/src/types.ts data/src/index.ts data/test/schema.test.ts package-lock.json
git commit -m "feat(data): Item/PricePoint/Source Zod şemaları"
```

---

## Task 3: Örnek veri (items, sources, prices)

**Files:**
- Create: `data/items/items.json`
- Create: `data/sources/sources.json`
- Create: `data/prices/gida.json`

- [ ] **Step 1: Kalem tanımlarını yaz**

`data/items/items.json`:
```json
[
  { "id": "ekmek", "isim": "Ekmek", "kategori": "gida", "birim": "adet", "ikon": "bread", "aciklama": "Standart somun ekmek" },
  { "id": "usd", "isim": "Amerikan Doları", "kategori": "doviz", "birim": "USD", "ikon": "currency-dollar", "aciklama": "1 USD karşılığı TL" },
  { "id": "gram-altin", "isim": "Gram Altın", "kategori": "altin", "birim": "gram", "ikon": "coin", "aciklama": "1 gram has altın" },
  { "id": "asgari-ucret", "isim": "Asgari Ücret", "kategori": "capa", "birim": "TL", "ikon": "wallet", "aciklama": "Net aylık asgari ücret" }
]
```

- [ ] **Step 2: Kaynakları yaz**

`data/sources/sources.json`:
```json
[
  { "ad": "TÜİK", "tip": "elle", "url": "https://www.tuik.gov.tr", "metodoloji": "Tüketici fiyatları ortalama değerleri" },
  { "ad": "TCMB EVDS", "tip": "resmi-api", "url": "https://evds2.tcmb.gov.tr", "metodoloji": "Günlük döviz ve altın kurları, yıllık ortalama" },
  { "ad": "Resmi Gazete", "tip": "elle", "url": "https://www.resmigazete.gov.tr", "metodoloji": "Asgari ücret tespit komisyonu kararları, net tutar" }
]
```

- [ ] **Step 3: Örnek fiyat noktalarını yaz**

`data/prices/gida.json`:
```json
[
  { "itemId": "ekmek", "yil": 2005, "deger": 0.4, "tip": "ortalama", "kaynakTipi": "elle", "kaynakAdi": "TÜİK", "kaynakURL": "https://www.tuik.gov.tr", "dogrulama": "beklemede", "not": "Örnek veri — doğrulanacak" },
  { "itemId": "ekmek", "yil": 2026, "deger": 25.0, "tip": "ortalama", "kaynakTipi": "elle", "kaynakAdi": "TÜİK", "kaynakURL": "https://www.tuik.gov.tr", "dogrulama": "beklemede", "not": "Örnek veri — doğrulanacak" },
  { "itemId": "asgari-ucret", "yil": 2005, "deger": 350.15, "tip": "net", "kaynakTipi": "elle", "kaynakAdi": "Resmi Gazete", "kaynakURL": "https://www.resmigazete.gov.tr", "dogrulama": "beklemede", "not": "Örnek veri — doğrulanacak" },
  { "itemId": "asgari-ucret", "yil": 2026, "deger": 22104.0, "tip": "net", "kaynakTipi": "elle", "kaynakAdi": "Resmi Gazete", "kaynakURL": "https://www.resmigazete.gov.tr", "dogrulama": "beklemede", "not": "Örnek veri — doğrulanacak" }
]
```

> Not: Değerler örnektir ve `dogrulama: beklemede` ile işaretlidir; gerçek kaynaklı veri Faz 2'de girilecek.

- [ ] **Step 4: Commit**

```bash
git add data/items/items.json data/sources/sources.json data/prices/gida.json
git commit -m "feat(data): örnek kalem, kaynak ve fiyat verileri"
```

---

## Task 4: data paketi — yükleyici + bütünlük doğrulaması (TDD)

**Files:**
- Create: `data/src/load.ts`
- Create: `data/src/validate.ts`
- Modify: `data/src/index.ts`
- Test: `data/test/validate.test.ts`

- [ ] **Step 1: Başarısız testi yaz**

`data/test/validate.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { ItemSchema, PricePointSchema } from "../src/schema";
import { validateDataset } from "../src/validate";

const items = [ItemSchema.parse({ id: "ekmek", isim: "Ekmek", kategori: "gida", birim: "adet", ikon: "bread", aciklama: "x" })];

const ok = [PricePointSchema.parse({ itemId: "ekmek", yil: 2005, deger: 0.4, tip: "ortalama", kaynakTipi: "elle", kaynakAdi: "TÜİK", kaynakURL: "https://www.tuik.gov.tr", dogrulama: "beklemede" })];

describe("validateDataset", () => {
  it("tutarlı veri için hata döndürmez", () => {
    const errors = validateDataset(items, ok);
    expect(errors).toEqual([]);
  });

  it("var olmayan itemId'yi yakalar", () => {
    const bad = [{ ...ok[0]!, itemId: "yok" }];
    const errors = validateDataset(items, bad);
    expect(errors.some((e) => e.includes("yok"))).toBe(true);
  });

  it("aynı item+yıl+tip için çift kaydı yakalar", () => {
    const dup = [ok[0]!, ok[0]!];
    const errors = validateDataset(items, dup);
    expect(errors.some((e) => e.includes("çift"))).toBe(true);
  });
});
```

- [ ] **Step 2: Testi çalıştır, başarısız olduğunu gör**

Run: `npx vitest run data/test/validate.test.ts`
Expected: FAIL — `../src/validate` bulunamıyor.

- [ ] **Step 3: Doğrulamayı yaz**

`data/src/validate.ts`:
```ts
import type { Item, PricePoint } from "./types";

export function validateDataset(items: Item[], prices: PricePoint[]): string[] {
  const errors: string[] = [];
  const itemIds = new Set(items.map((i) => i.id));

  for (const p of prices) {
    if (!itemIds.has(p.itemId)) {
      errors.push(`Fiyat noktası var olmayan kaleme bağlı: "${p.itemId}" (${p.yil})`);
    }
  }

  const seen = new Set<string>();
  for (const p of prices) {
    const key = `${p.itemId}|${p.yil}|${p.tip}|${p.kaynakAdi}`;
    if (seen.has(key)) {
      errors.push(`Çift kayıt: ${key}`);
    }
    seen.add(key);
  }

  return errors;
}
```

- [ ] **Step 4: Yükleyiciyi yaz**

> Not: `fs`/`import.meta.url` yerine **statik JSON import** kullanıyoruz. Next.js paketi bundle ederken `import.meta.url` yanlış yola çözülebilir; statik import hem Next hem vitest hem tsx altında güvenilir biçimde bundle olur. Yeni kategori eklemek = bir import + `priceFiles`'a bir satır.

`data/src/load.ts`:
```ts
import itemsJson from "../items/items.json";
import sourcesJson from "../sources/sources.json";
import gidaPrices from "../prices/gida.json";
import { ItemSchema, PricePointSchema, SourceSchema } from "./schema";
import type { Item, PricePoint, Source } from "./types";

export function loadItems(): Item[] {
  return (itemsJson as unknown[]).map((r) => ItemSchema.parse(r));
}

export function loadSources(): Source[] {
  return (sourcesJson as unknown[]).map((r) => SourceSchema.parse(r));
}

const priceFiles: unknown[][] = [gidaPrices as unknown[]];

export function loadPrices(): PricePoint[] {
  return priceFiles.flat().map((r) => PricePointSchema.parse(r));
}
```

- [ ] **Step 5: index.ts'i güncelle**

`data/src/index.ts` (tam içerik):
```ts
export { ItemSchema, PricePointSchema, SourceSchema, KATEGORILER, BIRIMLER } from "./schema";
export type { Item, PricePoint, Source } from "./types";
export { loadItems, loadSources, loadPrices } from "./load";
export { validateDataset } from "./validate";
```

- [ ] **Step 6: Testi çalıştır, geçtiğini gör**

Run: `npx vitest run data/test/validate.test.ts`
Expected: PASS (3 test).

- [ ] **Step 7: Commit**

```bash
git add data/src/load.ts data/src/validate.ts data/src/index.ts data/test/validate.test.ts
git commit -m "feat(data): veri yükleyici ve bütünlük doğrulaması"
```

---

## Task 5: Veri doğrulama CLI

**Files:**
- Create: `scripts/validate-data.ts`

- [ ] **Step 1: CLI scriptini yaz**

`scripts/validate-data.ts`:
```ts
import { loadItems, loadPrices, validateDataset } from "@ec/data";

function main() {
  const items = loadItems();
  const prices = loadPrices();
  const errors = validateDataset(items, prices);

  if (errors.length > 0) {
    console.error(`✗ ${errors.length} doğrulama hatası:`);
    for (const e of errors) console.error("  - " + e);
    process.exit(1);
  }
  console.log(`✓ ${items.length} kalem, ${prices.length} fiyat noktası — tüm veri tutarlı.`);
}

main();
```

- [ ] **Step 2: Çalıştır, başarılı olduğunu gör**

Run: `npm run validate:data`
Expected: `✓ 4 kalem, 4 fiyat noktası — tüm veri tutarlı.` ve çıkış kodu 0.

- [ ] **Step 3: Commit**

```bash
git add scripts/validate-data.ts
git commit -m "feat(scripts): veri doğrulama CLI"
```

---

## Task 6: Next.js web kabuğu

**Files:**
- Create: `web/` (create-next-app çıktısı)
- Modify: `web/package.json`
- Modify: `web/next.config.ts`

- [ ] **Step 1: Next.js uygulamasını oluştur**

Run:
```bash
npx create-next-app@latest web --typescript --tailwind --app --no-src-dir --eslint --use-npm --import-alias "@/*" --skip-git --turbopack
```
Expected: `web/` altında Next.js projesi oluşur. (İstem gelirse varsayılanları kabul et.)

- [ ] **Step 2: web içindeki ayrı lock dosyasını kaldır ve workspace adını ayarla**

Run: `rm -f web/package-lock.json`

`web/package.json` içinde `"name"` alanını `"@ec/web"` yap ve `dependencies`'e `@ec/data` ekle:
```json
"name": "@ec/web",
```
dependencies bloğuna ekle:
```json
"@ec/data": "*"
```

- [ ] **Step 3: next.config.ts'i workspace paketini derleyecek şekilde ayarla**

`web/next.config.ts` (tam içerik):
```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  transpilePackages: ["@ec/data"],
};

export default nextConfig;
```

- [ ] **Step 4: Workspace'i yeniden bağla**

Run: `npm install`
Expected: `web/node_modules/@ec/data` sembolik bağı oluşur, hata yok.

- [ ] **Step 5: Geliştirme sunucusunun açıldığını doğrula**

Run: `npm run dev -w web` (birkaç saniye sonra Ctrl+C)
Expected: "Ready on http://localhost:3000" benzeri çıktı, hata yok.

- [ ] **Step 6: Commit**

```bash
git add web package-lock.json
git commit -m "feat(web): Next.js kabuğu ve @ec/data entegrasyonu"
```

---

## Task 7: Feature flag config + AdSlot bileşeni (TDD)

**Files:**
- Create: `web/config/flags.ts`
- Create: `web/components/AdSlot.tsx`
- Create: `web/test/AdSlot.test.tsx`
- Modify: `web/package.json` (test bağımlılıkları + script)
- Modify: `web/vitest.config.ts`

- [ ] **Step 1: Test bağımlılıklarını kur**

Run:
```bash
npm install -D -w web vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom
```
Expected: web/package.json devDependencies'e eklenir.

- [ ] **Step 2: web vitest yapılandırması oluştur**

`web/vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: {
    include: ["test/**/*.test.tsx"],
    environment: "jsdom",
    globals: true,
  },
});
```

web/package.json `scripts`'e ekle:
```json
"test": "vitest run"
```

- [ ] **Step 3: flag config'i yaz**

`web/config/flags.ts`:
```ts
export const flags = {
  ads: { enabled: false },
  premium: { enabled: false },
  crowdsource: { enabled: false },
} as const;
```

- [ ] **Step 4: Başarısız testi yaz**

`web/test/AdSlot.test.tsx`:
```tsx
import { render } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { AdSlot } from "../components/AdSlot";

describe("AdSlot", () => {
  it("reklam flag'i kapalıyken hiçbir şey render etmez", () => {
    const { container } = render(<AdSlot id="anasayfa-ust" />);
    expect(container.firstChild).toBeNull();
  });
});
```

- [ ] **Step 5: Testi çalıştır, başarısız olduğunu gör**

Run: `npm run test -w web`
Expected: FAIL — `../components/AdSlot` bulunamıyor.

- [ ] **Step 6: AdSlot'u yaz**

`web/components/AdSlot.tsx`:
```tsx
import { flags } from "@/config/flags";

export function AdSlot({ id }: { id: string }) {
  if (!flags.ads.enabled) return null;
  return (
    <div data-ad-slot={id} className="my-4 flex min-h-[90px] items-center justify-center rounded-lg border border-dashed text-sm text-neutral-400">
      reklam alanı
    </div>
  );
}
```

- [ ] **Step 7: Testi çalıştır, geçtiğini gör**

Run: `npm run test -w web`
Expected: PASS (1 test).

- [ ] **Step 8: Commit**

```bash
git add web/config/flags.ts web/components/AdSlot.tsx web/test/AdSlot.test.tsx web/vitest.config.ts web/package.json package-lock.json
git commit -m "feat(web): feature flag config ve flag-korumalı AdSlot"
```

---

## Task 8: Ana sayfa — kalemleri listele + Kaynaklar sayfası

**Files:**
- Modify: `web/app/page.tsx`
- Create: `web/app/kaynaklar/page.tsx`

- [ ] **Step 1: Ana sayfayı yaz (veriyi @ec/data'dan yükler)**

`web/app/page.tsx` (tam içerik):
```tsx
import { loadItems } from "@ec/data";
import { AdSlot } from "@/components/AdSlot";

export default function Home() {
  const items = loadItems();
  return (
    <main className="mx-auto max-w-2xl p-6">
      <h1 className="text-2xl font-medium">Enflasyon Canavarı</h1>
      <p className="mt-2 text-neutral-500">Paranı canavar ne kadar yedi?</p>
      <AdSlot id="anasayfa-ust" />
      <ul className="mt-6 space-y-2">
        {items.map((i) => (
          <li key={i.id} className="rounded-lg border p-3">
            <span className="font-medium">{i.isim}</span>{" "}
            <span className="text-sm text-neutral-500">({i.kategori})</span>
          </li>
        ))}
      </ul>
      <a href="/kaynaklar" className="mt-6 inline-block text-sm underline">
        Kaynaklar ve metodoloji
      </a>
    </main>
  );
}
```

- [ ] **Step 2: Kaynaklar sayfasını yaz**

`web/app/kaynaklar/page.tsx` (tam içerik):
```tsx
import { loadSources } from "@ec/data";

export default function Kaynaklar() {
  const sources = loadSources();
  return (
    <main className="mx-auto max-w-2xl p-6">
      <h1 className="text-2xl font-medium">Kaynaklar ve metodoloji</h1>
      <p className="mt-2 text-neutral-500">
        Tüm fiyatlar yeni TL cinsindendir. Başlangıç: 1 Ocak 2005 (6 sıfırın atıldığı tarih).
        Değerler &quot;net&quot; veya &quot;ortalama&quot; olarak etiketlenir.
      </p>
      <ul className="mt-6 space-y-3">
        {sources.map((s) => (
          <li key={s.ad} className="rounded-lg border p-3">
            <div className="font-medium">{s.ad}</div>
            <div className="text-sm text-neutral-500">{s.metodoloji}</div>
            <a href={s.url} className="text-sm underline">{s.url}</a>
          </li>
        ))}
      </ul>
    </main>
  );
}
```

- [ ] **Step 3: Üretim derlemesinin geçtiğini doğrula**

Run: `npm run build -w web`
Expected: Derleme başarılı; `/` ve `/kaynaklar` rotaları üretilir, hata yok.

- [ ] **Step 4: Commit**

```bash
git add web/app/page.tsx web/app/kaynaklar/page.tsx
git commit -m "feat(web): kalem listesi olan ana sayfa ve kaynaklar sayfası"
```

---

## Task 9: docs notları + klasör README'leri

**Files:**
- Create: `docs/00-vizyon.md` … `docs/07-yol-haritasi.md`
- Create: `docs/kararlar/0001-stack-secimi.md`
- Create: `README.md`, `data/README.md`, `web/README.md`, `scripts/README.md`

- [ ] **Step 1: Kök README'yi yaz**

`README.md`:
```markdown
# Enflasyon Canavarı

Türkiye'deki enflasyon ve fiyat değişimini paylaşılabilir metriklerle anlatan web + (sonra) mobil uygulama.

## Yapı
- `web/` — Next.js frontend (`@ec/web`)
- `data/` — veri katmanı, şema ve doğrulama (`@ec/data`)
- `scripts/` — veri çekme/doğrulama araçları
- `docs/` — vizyon, mimari ve karar notları

## Komutlar
- `npm run dev` — web geliştirme sunucusu
- `npm test` — veri/şema testleri
- `npm run validate:data` — tüm veriyi doğrula

Tasarım dokümanı: `docs/superpowers/specs/2026-06-17-enflasyon-canavari-design.md`
```

- [ ] **Step 2: docs notlarını spec'ten türet**

Her dosya, spec'in ilgili bölümünün özetidir (kaynak: `docs/superpowers/specs/2026-06-17-enflasyon-canavari-design.md`):
- `docs/00-vizyon.md` — spec Bölüm 1 (vizyon, kimlik, marka B yönü)
- `docs/01-mimari.md` — spec Bölüm 2 (Next.js + Capacitor, workspaces, flag katmanı)
- `docs/02-veri-modeli.md` — spec Bölüm 3 (Item/PricePoint/Source, 2005 redenominasyon)
- `docs/03-veri-kaynaklari.md` — spec Bölüm 4 (hibrit sourcing, kalem listesi)
- `docs/04-ozellikler.md` — spec Bölüm 5 (4 MVP özelliği + UX)
- `docs/05-marka-tasarim.md` — spec Bölüm 1 marka + paylaşım kartı yönü
- `docs/06-gelir-flagler.md` — spec Bölüm 6 (reklam/premium flag modeli)
- `docs/07-yol-haritasi.md` — spec Bölüm 9 (fazlar)

Her dosya, ilgili bölümün başlığı + 1-2 paragraf özet + "Ayrıntı: spec Bölüm N" satırı içerir. Örnek `docs/07-yol-haritasi.md`:
```markdown
# Yol Haritası

1. Faz 0 — İskelet (bu plan): workspace, şema, doğrulama, örnek veri, web kabuğu.
2. Faz 1 — MVP web: yıl-yıl tablo, kalem-kalem karşılaştırma, OG paylaşım, kişisel hesap, kaynaklar.
3. Faz 2 — Veri genişletme: TCMB/EPDK çekme betikleri, kalem sayısını artırma.
4. Faz 3 — Store: Capacitor paketleme, iOS/Android.
5. Faz 4 — Gelir/crowdsource: flag'leri aç, premium/reklam, moderasyon.

Ayrıntı: docs/superpowers/specs/2026-06-17-enflasyon-canavari-design.md Bölüm 9.
```

- [ ] **Step 3: Karar günlüğünü yaz**

`docs/kararlar/0001-stack-secimi.md`:
```markdown
# 0001 — Teknoloji yığını seçimi

**Tarih:** 2026-06-17
**Durum:** Kabul edildi

## Karar
Web-first Next.js (App Router) + sonra Capacitor ile store. Veri katmanı npm workspace paketi (`@ec/data`).

## Gerekçe
Ürün paylaşım-önce; SEO ve OG link önizlemesi kritik. Next.js bunda en güçlü.
Flutter/RN web SEO ve OG tarafında zayıf. Capacitor bir veri/karşılaştırma uygulamasını
store'a taşımak için düşük riskli.

## Alternatifler
- Flutter: web SEO/OG zayıf — reddedildi.
- Expo/React Native: web tarafı manuel iş — reddedildi.
```

- [ ] **Step 4: Klasör README'lerini yaz**

`data/README.md`:
```markdown
# @ec/data — veri katmanı

Kalem (`items/`), kaynak (`sources/`) ve fiyat (`prices/`) JSON verileri + Zod şemaları (`src/schema.ts`) ve doğrulama (`src/validate.ts`).

## Yeni fiyat eklemek
1. İlgili `prices/<kategori>.json` dosyasına nokta ekle (kaynak ve tip zorunlu).
2. Yeni bir kategori dosyası açtıysan `src/load.ts` içine import edip `priceFiles`'a ekle.
3. `npm run validate:data` çalıştır — hata yoksa tamam.
```

`web/README.md`:
```markdown
# @ec/web — frontend

Next.js App Router uygulaması. Veriyi `@ec/data`'dan yükler. Reklam/premium `config/flags.ts` ile yönetilir (varsayılan kapalı).
```

`scripts/README.md`:
```markdown
# scripts — veri araçları

- `validate-data.ts` — tüm veri setini şema + bütünlük açısından doğrular (`npm run validate:data`).
```

- [ ] **Step 5: Tüm testlerin ve doğrulamanın geçtiğini son kez doğrula**

Run: `npm test && npm run validate:data && npm run test -w web`
Expected: Hepsi PASS / `✓`.

- [ ] **Step 6: Commit**

```bash
git add README.md docs
git commit -m "docs: vizyon/mimari notları, karar günlüğü ve klasör README'leri"
```

---

## Tamamlanma Kriterleri (Faz 0)

- [ ] `npm install` temiz çalışıyor (workspaces bağlı).
- [ ] `npm test` → data şema + doğrulama testleri geçiyor.
- [ ] `npm run validate:data` → "tüm veri tutarlı" çıktısı.
- [ ] `npm run test -w web` → AdSlot testi geçiyor.
- [ ] `npm run build -w web` → üretim derlemesi başarılı.
- [ ] `npm run dev` → ana sayfa kalemleri, `/kaynaklar` kaynakları listeliyor.
- [ ] `docs/` notları ve klasör README'leri yerinde.

## Sonraki Planlar (bu planın kapsamı dışı)
- Plan 2 — Yıl-yıl fiyat tablosu + grafik
- Plan 3 — Kalem-kalem satın alma gücü karşılaştırması + hesap motoru (TDD)
- Plan 4 — OG paylaşım kartı üretimi (`@vercel/og`)
- Plan 5 — Kişisel maaş/para hesabı
