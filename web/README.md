# @ec/web — frontend

Next.js App Router uygulaması. Veriyi `@ec/data`'dan yükler. Reklam/premium `config/flags.ts` ile yönetilir (varsayılan kapalı).

## Komutlar
- `npm run dev -w web` — geliştirme sunucusu
- `npm run build -w web` — üretim derlemesi
- `npm run test -w web` — testler (Vitest)
